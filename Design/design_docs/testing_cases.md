[ &#127968; Return To Design Info](../design_info.md)

# Test Cases
This table lists the backend integration test cases derived from the Jest/Supertest files in `/backend/tests/integration`.
- **Test ID**: Unique identifier (e.g., `CAT-GET-01`).
- **Test Description**: Brief description from the test (`it` block).
- **Input Data**: HTTP Method, Path, Request Body/Params, and Auth context (Admin/User/None).
- **Expected Result**: Expected HTTP status and key aspects of the response body/structure.
- **Actual Result**: Outcome of the test run.
- **Status**: Pass/Fail.

| Test ID             | Status | Test Description                                             | Input Data                                                                 | Expected Result                                        | Actual Result                                          |
|---------------------|--------|--------------------------------------------------------------|----------------------------------------------------------------------------|--------------------------------------------------------|--------------------------------------------------------|
| **Categories** |        |                                                              |                                                                            |                                                        |                                                        |
| CAT-GET-01          | Pass   | GET should get all categories                                | GET `/categories` (Admin Auth)                                             | Status 200, Returns array of categories                | Status 200, Returns array of categories                |
| CAT-POST-01         | Pass   | POST should create a new category                            | POST `/categories`, Body: `{ name, description }` (Admin Auth)             | Status 201, Returns new category object with ID        | Status 201, Returns new category object with ID        |
| CAT-PUT-01          | Pass   | PUT should update a category                                 | PUT `/categories/:id`, Body: `{ id, name, description }` (Admin Auth)      | Status 200, Returns updated category object            | Status 200, Returns updated category object            |
| CAT-DELETE-01       | Pass   | DELETE should delete a category                              | DELETE `/categories/:id` (Admin Auth)                                      | Status 204 (No Content)                                | Status 204 (No Content)                                |
| **Admins** |        |                                                              |                                                                            |                                                        |                                                        |
| ADMIN-POST-01       | Pass   | should add an admin to the database                          | POST `/auth/admin`, Body: `{ email: "aTestAdmin@email.com" }` (Admin Auth) | Status 200, Body: `{ message: "Admin created" }`       | Status 200, Body: `{ message: "Admin created" }`       |
| ADMIN-POST-404      | Pass   | should return 404 if admin already exists                    | POST `/auth/admin`, Body: `{ email: <existing_admin_email> }` (Admin Auth) | Status 404, Body: `{ message: "Admin already exists" }`| Status 404, Body: `{ message: "Admin already exists" }`|
| ADMIN-POST-ERR-500  | Pass   | should return 500 if server error occurs (POST)              | POST `/auth/admin`, Body: `{ email }`, Mock `adminModel.addAdmin` error    | Status 500, Body: `{ message: "Server Error" }`        | Status 500, Body: `{ message: "Server Error" }`        |
| ADMIN-DELETE-01     | Pass   | should remove an admin from the database                     | DELETE `/auth/admin`, Body: `{ email: <admin_to_delete> }` (Admin Auth)    | Status 200, Body: `{ message: "Admin removed" }`       | Status 200, Body: `{ message: "Admin removed" }`       |
| ADMIN-DELETE-404    | Pass   | should return 404 if admin does not exist                    | DELETE `/auth/admin`, Body: `{ email: <non_existent_email> }` (Admin Auth) | Status 404, Body: `{ message: "Email not found" }`     | Status 404, Body: `{ message: "Email not found" }`     |
| ADMIN-DELETE-ERR-500| Pass   | should return 500 if server error occurs (DELETE)            | DELETE `/auth/admin`, Body: `{ email }`, Mock `adminModel.deleteAdmin` error | Status 500, Body: `{ message: "Server Error" }`        | Status 500, Body: `{ message: "Server Error" }`        |
| **Discounts (General)** |      |                                                          |                                                                            |                                                        |                                                        |
| DISC-GET-ALL-01     | Pass   | should get all discounts                                     | GET `/discounts` (Admin Auth)                                              | Status 200, Returns array of discounts                 | Status 200, Returns array of discounts                 |
| DISC-GET-ALL-LIMIT  | Pass   | Should return a specific number of discounts based on limit  | GET `/discounts?limit=1` (Admin Auth)                                      | Status 200, Returns array with 1 discount              | Status 200, Returns array with 1 discount              |
| DISC-GET-ALL-CAT    | Pass   | Should return a specific number based on category ID         | GET `/discounts?categoryId=<id>` (Admin Auth)                              | Status 200, Returns array of relevant discounts        | Status 200, Returns array of relevant discounts        |
| DISC-GET-ALL-PAGE   | Pass   | should return a specific number of discounts based on page   | GET `/discounts?limit=1&page=2` (Admin Auth)                               | Status 200, Returns array with 1 discount (page 2)     | Status 200, Returns array with 1 discount (page 2)     |
| DISC-GET-ALL-404    | Pass   | should return 404 status code if no discounts are found      | GET `/discounts`, Mock `discountModel.getDiscounts` returns `[]`           | Status 404, Body: `{ message: "No discounts found." }` | Status 404, Body: `{ message: "No discounts found." }` |
| DISC-GET-ALL-ERR-500| Pass   | should return 500 status code if an error occurs (GET All)   | GET `/discounts`, Mock `discountModel.getDiscounts` error                  | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| DISC-POST-01        | Pass   | should create a new discount                                 | POST `/discounts`, Body: `{ code, percentOff, ... }` (Admin Auth)          | Status 200, Returns new discount object with ID        | Status 200, Returns new discount object with ID        |
| DISC-POST-400       | Pass   | should return 400 if percentOff is not provided              | POST `/discounts`, Body: `{ code, ... }` (Admin Auth)                      | Status 400, Body: `{ message: "Invalid request..." }`  | Status 400, Body: `{ message: "Invalid request..." }`  |
| DISC-POST-ERR-500   | Pass   | should return 500 status code if an error occurs (POST)      | POST `/discounts`, Body: `{ percentOff }`, Mock `discountModel.createDiscount` error | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| DISC-GET-ID-01      | Pass   | should get a discount by id                                  | GET `/discounts/:id` (Admin Auth)                                          | Status 200, Returns discount object                    | Status 200, Returns discount object                    |
| DISC-GET-ID-404     | Pass   | should return 404 status code if discount is not found       | GET `/discounts/9999` (Admin Auth)                                         | Status 404, Body: `{ message: "Discount not found." }` | Status 404, Body: `{ message: "Discount not found." }` |
| DISC-GET-ID-ERR-500 | Pass   | should return 500 status code if an error occurs (GET ID)    | GET `/discounts/9999`, Mock `discountModel.getDiscountById` error          | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| DISC-PUT-ID-01      | Pass   | should update a discount by id                               | PUT `/discounts/:id`, Body: `{ code, percentOff, ... }` (Admin Auth)       | Status 200, Returns updated discount object            | Status 200, Returns updated discount object            |
| DISC-PUT-ID-404     | Pass   | should return 404 status code if discount is not found (PUT) | PUT `/discounts/9999`, Body: `{ ... }` (Admin Auth)                        | Status 404, Body: `{ message: "Discount not found." }` | Status 404, Body: `{ message: "Discount not found." }` |
| DISC-PUT-ID-ERR-500 | Pass   | should return 500 status code if an error occurs (PUT)       | PUT `/discounts/9999`, Body: `{ ... }`, Mock `discountModel.updateDiscount` error | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| DISC-DELETE-ID-01   | Pass   | should delete a discount by id                               | DELETE `/discounts/:id` (Admin Auth)                                       | Status 200, Body: `{ message: "Discount deleted." }`   | Status 200, Body: `{ message: "Discount deleted." }`   |
| DISC-DELETE-ID-404  | Pass   | should return 404 status code if discount is not found (DEL) | DELETE `/discounts/9999` (Admin Auth)                                      | Status 404, Body: `{ message: "Discount not found." }` | Status 404, Body: `{ message: "Discount not found." }` |
| DISC-DELETE-ID-ERR-500| Pass   | should return 500 status code if an error occurs (DELETE)  | DELETE `/discounts/9999`, Mock `discountModel.deleteDiscount` error        | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| **Discounts (Code)**|        |                                                              |                                                                            |                                                        |                                                        |
| DISC-CODE-POST-01   | Pass   | should return a formatted discount that relates to the code  | POST `/discounts/code/DISCOUNT10` (Admin Auth)                             | Status 200, Returns discount object for code           | Status 200, Returns discount object for code           |
| DISC-CODE-POST-404  | Pass   | should return status 404 if discount code does not exist     | POST `/discounts/code/DOESNOTEXIST` (Admin Auth)                           | Status 404, Body: `{ message: "Discount not found." }` | Status 404, Body: `{ message: "Discount not found." }` |
| DISC-CODE-POST-ERR-500| Pass   | should return status 500 if server error occurs            | POST `/discounts/code/DISCOUNT10`, Mock `discountModel.getDiscountByCode` error | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| **Discounts (Products)**|      |                                                          |                                                                            |                                                        |                                                        |
| DISC-PROD-GET-01    | Pass   | should get all products with discounts                       | GET `/discounts/products` (Admin Auth)                                     | Status 200, Returns array of discounted products       | Status 200, Returns array of discounted products       |
| DISC-PROD-GET-LIMIT | Pass   | should return a specific number of products based on limit   | GET `/discounts/products?limit=1` (Admin Auth)                             | Status 200, Returns array with 1 product               | Status 200, Returns array with 1 product               |
| DISC-PROD-GET-PAGE  | Pass   | should return a specific number of products based on page    | GET `/discounts/products?limit=1&page=1` (Admin Auth)                      | Status 200, Returns array with 1 product (page 1)      | Status 200, Returns array with 1 product (page 1)      |
| DISC-PROD-GET-404   | Pass   | should return 404 if no products with discounts found        | GET `/discounts/products`, Mock `discountModel.getDiscountedProducts` returns `[]` | Status 404, Body: `{ message: "No discounted..." }`    | Status 404, Body: `{ message: "No discounted..." }`    |
| DISC-PROD-GET-ERR-500| Pass   | should return 500 status code if an error occurs (GET Prod)  | GET `/discounts/products`, Mock `discountModel.getDiscountedProducts` error | Status 500, Body: `{ message: "Server error: ..." }`   | Status 500, Body: `{ message: "Server error: ..." }`   |
| DISC-PROD-POST-01   | Pass   | should add a discount to a product                           | POST `/discounts/products`, Body: `{ productId, discountId }` (Admin Auth) | Status 200, Returns new product_discount link object   | Status 200, Returns new product_discount link object   |
| DISC-PROD-POST-ERR-500| Pass   | should return 500 status code if an error occurs (POST Prod)| POST `/discounts/products`, Body: `{...}`, Mock `discountModel.addDiscountToProduct` error | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| DISC-PROD-DELETE-ID | Pass   | should delete a discount from product by productDiscountId   | DELETE `/discounts/products`, Body: `{ productDiscountId }` (Admin Auth)   | Status 200, Body: `{ message: "Discount removed..." }` | Status 200, Body: `{ message: "Discount removed..." }` |
| DISC-PROD-DELETE-PAIR| Pass   | should delete a discount from product by productId/discountId| DELETE `/discounts/products`, Body: `{ productId, discountId }` (Admin Auth)| Status 200, Body: `{ message: "Discount removed..." }` | Status 200, Body: `{ message: "Discount removed..." }` |
| DISC-PROD-DELETE-404| Pass   | should return 404 if no discount on product is found         | DELETE `/discounts/products`, Body: `{ productId: 9999, ... }` (Admin Auth)| Status 404, Body: `{ message: "Discount not found..." }`| Status 404, Body: `{ message: "Discount not found..." }`|
| DISC-PROD-DELETE-400| Pass   | should return 400 status code if invalid request object      | DELETE `/discounts/products`, Body: `{ productId: 1 }` (Admin Auth)        | Status 400, Body: `{ message: "Invalid request..." }`  | Status 400, Body: `{ message: "Invalid request..." }`  |
| DISC-PROD-DELETE-ERR-500| Pass   | should return 500 status code if an error occurs (DEL Prod)| DELETE `/discounts/products`, Body: `{...}`, Mock `discountModel.removeDiscountFromProduct` error | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| **Heros** |        |                                                              |                                                                            |                                                        |                                                        |
| HERO-GET-ALL-01     | Pass   | should return all heros                                      | GET `/heros` (Admin Auth)                                                  | Status 200, Returns array of hero objects              | Status 200, Returns array of hero objects              |
| HERO-GET-ALL-ERR-500| Pass   | should return 500 status code if there is an error (GET All) | GET `/heros`, Mock `herosModel.getAllHeros` error                          | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| HERO-GET-ALL-404    | Pass   | should return 404 status code if there are no heros          | GET `/heros`, Mock `herosModel.getAllHeros` returns `[]`                   | Status 404, Body: `{ message: "No heros found." }`     | Status 404, Body: `{ message: "No heros found." }`     |
| HERO-POST-01        | Pass   | should create a new hero with a product                      | POST `/heros`, Body: `{ productId, categoryId, layout, ... }` (Admin Auth) | Status 201, Returns new hero object with ID            | Status 201, Returns new hero object with ID            |
| HERO-POST-403       | Pass   | should return 403 status code if there is a validation error | POST `/heros`, Body: `{ ... }` (Non-Admin Auth)                            | Status 403, Body: `{ message: "Access denied" }`       | Status 403, Body: `{ message: "Access denied" }`       |
| HERO-POST-ERR-500   | Pass   | should return 500 status code if there is an error (POST)    | POST `/heros`, Body: `{ ... }`, Mock `herosModel.createHero` error         | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| HERO-POST-400-EMPTY | Pass   | should return 400 status code if the object is empty         | POST `/heros`, Body: `{}` (Admin Auth)                                     | Status 400, Body: `{ message: "Bad Request: ..." }`    | Status 400, Body: `{ message: "Bad Request: ..." }`    |
| HERO-POST-400-MISS  | Pass   | should return 400 status code if missing required fields     | POST `/heros`, Body: `{ layout, heading, ... }` (Admin Auth)               | Status 400, Body: `{ message: "Bad Request: ..." }`    | Status 400, Body: `{ message: "Bad Request: ..." }`    |
| HERO-PUT-ID-01      | Pass   | should update a hero                                         | PUT `/heros/:id`, Body: `{ layout, heading, ... }` (Admin Auth)            | Status 200, Returns updated hero object                | Status 200, Returns updated hero object                |
| HERO-PUT-ID-404     | Pass   | should return 404 status code if hero is not found           | PUT `/heros/999`, Body: `{ ... }` (Admin Auth)                             | Status 404, Body: `{ message: "Hero not found." }`     | Status 404, Body: `{ message: "Hero not found." }`     |
| HERO-PUT-ID-403     | Pass   | should return 403 status code if validation error (PUT)      | PUT `/heros/1`, Body: `{ ... }` (Non-Admin Auth)                           | Status 403, Body: `{ message: "Access denied" }`       | Status 403, Body: `{ message: "Access denied" }`       |
| HERO-PUT-ID-ERR-500 | Pass   | should return 500 status code if there is an error (PUT)     | PUT `/heros/:id`, Body: `{ ... }`, Mock `herosModel.updateHero` error      | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| HERO-DELETE-ID-01   | Pass   | should delete a hero                                         | DELETE `/heros/:id` (Admin Auth)                                           | Status 200, Body: `{ message: "Hero deleted..." }`     | Status 200, Body: `{ message: "Hero deleted..." }`     |
| HERO-DELETE-ID-404  | Pass   | should return 404 status code if hero is not found (DELETE)  | DELETE `/heros/999` (Admin Auth)                                           | Status 404, Body: `{ message: "Hero not found." }`     | Status 404, Body: `{ message: "Hero not found." }`     |
| HERO-DELETE-ID-403  | Pass   | should return 403 status code if validation error (DELETE)   | DELETE `/heros/1` (Non-Admin Auth)                                         | Status 403, Body: `{ message: "Access denied" }`       | Status 403, Body: `{ message: "Access denied" }`       |
| HERO-DELETE-ID-ERR-500| Pass   | should return 500 status code if there is an error (DELETE)| DELETE `/heros/1`, Mock `herosModel.deleteHero` error                      | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| HERO-GET-IMAGE-URL-01 |    | should return the image URL for a hero                     | GET `/heros/:id` (Admin auth)                                              | Body: to contain `imageUrl` field                      |                       |
| **Users (Admin)** |        |                                                              |                                                                            |                                                        |                                                        |
| USER-ADMIN-GET-ID-01| Pass   | should get the user's object only if the user is an admin    | GET `/user/:email` (Admin Auth)                                            | Status 200, Returns user object                        | Status 200, Returns user object                        |
| USER-ADMIN-DEL-ID-01| Pass   | should return 200 if the user is deleted                     | DELETE `/user/:email` (Admin Auth)                                         | Status 200                                             | Status 200                                             |
| **Users (Not Logged In)**|      |                                                         |                                                                            |                                                        |                                                        |
| USER-REC-VERIFY-01  | Pass   | should return status:verified if code is correct             | POST `/user/recovery/verify`, Body: `{ code: <valid_code> }` (No Auth)     | Status 200, Body: `{ message: "verified" }`            | Status 200, Body: `{ message: "verified" }`            |
| USER-REC-VERIFY-02  | Pass   | should return status 400:unverified if code is incorrect     | POST `/user/recovery/verify`, Body: `{ code: "notAcode" }` (No Auth)       | Status 400, Body: `{ message: "unverified" }`          | Status 400, Body: `{ message: "unverified" }`          |
| USER-REC-VERIFY-03  | Pass   | should return status 400 if time has expired                 | POST `/user/recovery/verify`, Body: `{ code: <expired_code> }` (No Auth)   | Status 400, Body: `{ message: "unverified" }`          | Status 400, Body: `{ message: "unverified" }`          |
| USER-REC-VERIFY-04  | Pass   | should return status 400 if missing code                     | POST `/user/recovery/verify`, Body: `{}` (No Auth)                         | Status 400, Body: `{ message: "Bad Request: ..." }`    | Status 400, Body: `{ message: "Bad Request: ..." }`    |
| USER-REC-VERIFY-ERR-01| Pass   | should return status 500 if server error (Verify)          | POST `/user/recovery/verify`, Body: `{ code }`, Mock `userModel.verifyPasswordCode` error | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| USER-REC-REQ-01     | Pass   | should generate a password code for the user's email         | POST `/user/recovery`, Body: `{ email: <valid_email> }` (No Auth)          | Status 200, Code generated in DB                       | Status 200, Code generated in DB                       |
| USER-REC-REQ-02     | Pass   | should return status 404 if the email is not found           | POST `/user/recovery`, Body: `{ email: "notAnEmail" }` (No Auth)           | Status 404, Body: `{ message: "Email not found." }`    | Status 404, Body: `{ message: "Email not found." }`    |
| USER-REC-REQ-03     | Pass   | should return status 400 if missing email                    | POST `/user/recovery`, Body: `{}` (No Auth)                                | Status 400, Body: `{ message: "Bad Request: ..." }`    | Status 400, Body: `{ message: "Bad Request: ..." }`    |
| USER-REC-REQ-ERR-01 | Pass   | should return status 500 if server error (Request)           | POST `/user/recovery`, Body: `{ email }`, Mock `userModel.setPasswordRecovery` error | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| USER-REC-UPDATE-01  | Pass   | should change password with correct code & remove code       | POST `/user/recovery/update`, Body: `{ code, password }` (No Auth)         | Status 200, Body: `{ message: "Password updated" }`    | Status 200, Body: `{ message: "Password updated" }`    |
| USER-REC-UPDATE-02  | Pass   | should return 400 if the code is incorrect                   | POST `/user/recovery/update`, Body: `{ code: "notAcode", ... }` (No Auth)  | Status 400, Body: `{ message: "unverified" }`          | Status 400, Body: `{ message: "unverified" }`          |
| USER-REC-UPDATE-ERR-01| Pass   | should return status 500 if server error (Update)          | POST `/user/recovery/update`, Body: `{...}`, Mock `userModel.updatePasswordWithRecovery` error | Status 500, Body: `{ message: "Server Error: ..." }`   | Status 500, Body: `{ message: "Server Error: ..." }`   |
| USER-REG-01         | Pass   | should return 200 if user is created                         | POST `/user/register`, Body: `{ <valid_user_data> }` (No Auth)             | Status 200, Body: `{ message: "logged in" }`           | Status 200, Body: `{ message: "logged in" }`           |
| USER-REG-02         | Pass   | should return 400 if email is missing                        | POST `/user/register`, Body: `{ <missing_email> }` (No Auth)               | Status 400, Body: `{ message: "Bad Request: ..." }`    | Status 400, Body: `{ message: "Bad Request: ..." }`    |
| USER-REG-03         | Pass   | should return 422 if email is invalid                        | POST `/user/register`, Body: `{ email: "invalid", ... }` (No Auth)         | Status 422, Body: `{ message: "Unprocessable..." }`    | Status 422, Body: `{ message: "Unprocessable..." }`    |
| USER-REG-04         | Pass   | should return 400 if username is missing                     | POST `/user/register`, Body: `{ <missing_username> }` (No Auth)            | Status 400, Body: `{ message: "Bad Request: ..." }`    | Status 400, Body: `{ message: "Bad Request: ..." }`    |
| USER-REG-05         | Pass   | should return 400 if password is missing                     | POST `/user/register`, Body: `{ <missing_password> }` (No Auth)            | Status 400, Body: `{ message: "Bad Request: ..." }`    | Status 400, Body: `{ message: "Bad Request: ..." }`    |
| USER-REG-06         | Pass   | should return 400 if password is invalid                     | POST `/user/register`, Body: `{ password: "short", ... }` (No Auth)        | Status 400, Body: `{ message: "Bad Request: ..." }`    | Status 400, Body: `{ message: "Bad Request: ..." }`    |
| USER-REG-07         | Pass   | should return 409 if email already exists                    | POST `/user/register`, Body: `{ email: <existing_email>, ... }` (No Auth)  | Status 409, Body: `{ message: "Email already in use" }`| Status 409, Body: `{ message: "Email already in use" }`|
| USER-GET-ID-AUTH-01 | Pass   | should return 401 if the user is not an admin (GET ID)       | GET `/user/:email` (No Auth)                                               | Status 401                                             | Status 401                                             |
| USER-PUT-AUTH-01    | Pass   | should return 401 if the user is not logged in (PUT)         | PUT `/user` (No Auth)                                                      | Status 401                                             | Status 401                                             |
| USER-DELETE-ID-AUTH-01| Pass   | should return 401 if the user is not an admin (DELETE ID)  | DELETE `/user/:email` (No Auth)                                            | Status 401                                             | Status 401                                             |
| **Users (Logged In)**|        |                                                             |                                                                            |                                                        |                                                        |
| USER-GET-ID-403     | Pass   | should return 403 if the user is not an admin (GET ID)       | GET `/user/:other_email` (User Auth)                                       | Status 403                                             | Status 403                                             |
| USER-GET-SELF-01    | Pass   | should get the user's object only if the user is same user   | GET `/user` (User Auth)                                                    | Status 200, Returns own user object                    | Status 200, Returns own user object                    |
| USER-PUT-SELF-01    | Pass   | should update a user's object if the user is same user       | PUT `/user`, Body: `{ email, username, name, password, ... }` (User Auth) | Status 200, Returns updated user object                | Status 200, Returns updated user object                |
| USER-DELETE-SELF-01 | Pass   | should delete a user's object if the user is same user       | DELETE `/user` (User Auth)                                                 | Status 200                                             | Status 200                                             |
| **Products** ||||||